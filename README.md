## Repolyzer

GitHub repository viewer and analyzer workspace.

### Scripts

```bash
npm --prefix api install
npm --prefix web install

npm run dev:api
npm run dev:web
```

The API starts on `http://localhost:8080` and exposes `GET /health` plus `GET /api/health`.
